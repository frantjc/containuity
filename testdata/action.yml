name: test
description: test action yaml
inputs:
  test:
    description: some test input
outputs:
  test:
    description: test output
runs:
  using: docker
  image: docker://alpine
  args:
    - env
